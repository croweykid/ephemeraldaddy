TRAITS = {

    "does comedy well": {
        "name": "does comedy well",
        "signs": {},
        "antisigns": {},
        "houses": {
            "11": 4,
            "7": -3,
        },
        "antihouses": {
            "12": 7,
            "9": 4, #dogmatic, serious - found in 'most solemn gem suns'
        },
        "bodies": {},
        "antibodies": {}, #Saturn is in 'most solemn' but not 'humorless'.
        "nakshatras": {
            "Magha": 4,
            "Rohini": 3,
            "Bharani": -4, #never tries to be funny
        },
        "antinakshatras": {
            "Ardra": -6 #never 'humorless', but often 'solemn'/caustic
        },
        "positions": {
            "Sun in Gemini": 4,
            "Sun in H10": 5,
            "Mercury in Scorpio": -3,
            "Mercury in Capricorn": -3,
            "Saturn in H11": 5,
            "Uranus in Virgo": 5,
            #"Neptune in Taurus": -1,
            #"Neptune in Scorpio": 7,
            #"Neptune in Sagittarius": 4,
            "Neptune in H3": 4,
            #"Pluto in Virgo": 7,
            #"Pluto in Libra": 5,
            #"Pluto in Sagittarius": -3,
            "Ceres in Leo": 3,
            "Ceres in H5": 3,
            "Gemini in H11": 4,
            "Sagittarius in H5": 4,
            "True Lilith in H7": -5,
        },
        "antipositions": {
            "Sun in Virgo": 5,
            "Sun in Aquarius": -5,
#            "Moon in Sagittarius": 5, #Decan 3 (Leo/Sun) is twice as hilarious as Decan 1 (Sag/Jupiter).
            "Mercury in Libra": 6,
            "Mars in Scorpio": 5,
            "Saturn in Cancer": -5,
#            "Uranus in Cancer": 6,
#            "Pluto in Leo": 10,
#            "Pluto in Libra": -7,
            "Chiron in Aries": -9,
            "Chiron in Sagittarius": 7,
            "Ceres in H2": -6,
            "Ceres in H3": 8,
            "Juno in Leo": 4,
            "Juno in H10": 8,
            "Part of Fortune in H5": -6,
            "True Lilith in H10": 7,
            "True Lilith in H12": 6
        },
        "aspects": {
            "Neptune sextile Pluto": 12,
            "Chiron opposition Uranus": 7,
            #"Chiron trine Neptune": 4,
            "Chiron opposition Pluto": 3,
            "Pluto conjunction Uranus": 5,
            #"Neptune sextile Uranus": 3,
            "Ceres conjunction Juno": 3,
            "Uranus sextile Vesta": 3,
            "IC opposition Jupiter": 3,
            "Jupiter conjunction MC": 3,
            "AS conjunction Pluto": 3,
            "DS opposition Pluto": 3,
            "DS quincunx Mars": 3,
            "DS quincunx Saturn": 3,
            "AS trine Pallas": -3,
        },
        "antiaspects": {
            #these came over from 'solemn gem suns'
            "Neptune opposition Sun": 18, #shows up in funny charts, shows up WAY more in solemn charts
            "Chiron trine Neptune": 17, #shows up in funny charts but shows up MORE in solemn charts
            "Juno conjunction Pallas": 17,
            "AS trine Ceres": 17,
            "Mercury square Pallas": 16,
            "Juno trine Sun": 15,
            "Pluto trine Saturn": 14,
            "Moon square Saturn": 12,
            #these came over from 'humorless'
            "Pluto square Sun": 12,
            "Chiron opposition Uranus": -8,
            "Ceres square True Lilith": 6,
            "Juno sextile Sun": 6,
            "Mars trine Pallas": 5,
            "Part of Fortune trine True Lilith": 5,
            "Uranus square Venus": 5,
            "Ceres square Uranus": 5,
            "Mars trine Saturn": 4,
            "Moon sextile True Lilith": 5,
            "Rahu trine Uranus": 5,
            "IC square Mars": 9,
            "MC square Mars": 9,
            "Juno quincunx Rahu": 6,
            "Pallas conjunction Sun": 4,
            "Chiron trine Pluto": 4,
            "Juno semisextile Ketu": 5,
            "Mars quincunx Uranus": 5,
            "Mars semisquare Venus": 3,
            "Ceres sesquiquadrate Moon": 4,
            "Juno semisquare Neptune": 4,
            "Mars biquintile Pluto": 4,
            "Mercury sesquiquadrate Pluto": 4,
            "Pluto semisextile Venus": 4,
            "Chiron biquintile Moon": 3,
            "Chiron semisextile Part of Fortune": 3,
            "DS quincunx Jupiter": 4,
            "Juno quintile Venus": 3,
            "Juno semisextile Mercury": 3,
            "Ketu quincunx Mars": 3,
            "Ketu sesquiquadrate Pallas": 3,
            "Mars semisquare Pallas": 3,
            "Pallas semisquare Rahu": 3,
            "Pallas sesquiquadrate Pluto": 3,
            "Sun biquintile True Lilith": 3,
            "Chiron quintile Pluto": 3,
            "Jupiter semisquare Pallas": 3,
            "Jupiter sesquiquadrate Mercury": 3,
            "Ketu quintile Mars": 3,
            "Mars sesquiquadrate True Lilith": 3,
            "Mercury biquintile Rahu": 3,
            "Mercury semisquare Uranus": 3,
            "Moon biquintile Pallas": 3,
            "Moon sesquiquadrate Pluto": 3,
            "Neptune semisextile Sun": 3,
            "Pallas semisquare Venus": 3,
            "Part of Fortune quintile Pluto": 3,
            "AS quintile Rahu": 4,
            "IC sesquiquadrate Sun": 4,
            "Mars conjunction Mercury": -5,
            "Mars semisextile Rahu": 3,
            "MC semisquare Sun": 4,
            "AS semisextile Mercury": 4,
            "AS sesquiquadrate Vesta": 3,
            "Chiron quintile DS": 3,
            "Chiron semisquare Neptune": 3,
            "DS quintile Uranus": 3,
            "DS semisquare Vesta": 3,
            "IC quincunx Neptune": 3,
            "Jupiter quintile MC": 3,
            "Jupiter square Pallas": -4,
            "Ketu square Neptune": -5,
            "Mars conjunction Sun": -4,
            "Neptune square Rahu": -5,
            "Ceres conjunction Juno": -5,
            "Ceres sextile Saturn": -4,
            "Ceres square Venus": -4,
            "DS trine Saturn": -6,
            "Juno conjunction Pluto": -5,
            "IC trine Pallas": -7,
            "Ketu square Sun": -6,
            "MC trine Neptune": -8,
            "Mercury conjunction Vesta": -5,
            "Rahu square Sun": -6
        },
        "gates": {
            34: 5,
            20: 5,
            19: -4,
        },
        "antigates": {},
        "channels": {
            (20, 34): 4,
        },
        "antichannels": {
            (16, 48): 5
        },
        "centers": {},
        "anticenters": {},
        "profiles": {},
        "antiprofiles": {},
        "authorities": {
            "Splenic": -3,
        },
        "antiauthorities": {
            "Splenic": 8
        },
        "bazisigns": {},
        "antibazisigns": {},
        "color": "#cc99ff",
        "motivation": "",
        "description": "",
        "quotes": {},
    },
    "conflict_avoidant":{
     "name":"Conflict Avoidant",
        "signs":{"Capricorn":10,"Sagittarius":9,"Libra":7,"Gemini":4,"Cancer":4}, #Pretty sure Sagittarius is just trying not to go to jail...again. But maybe it's a specific nakshatra of Sag that's calling those shots. Also-- no pisces??
        "antisigns":{"Taurus":8,"Leo":6,"Scorpio":4},
        "houses":{5:6,11:4,4:3},
        "antihouses":{1:5,2:4},
        "bodies":{"Pluto":3}, #they've seen too much already...
        "antibodies":{},
        "nakshatras":{"Hasta":8,"Uttara Ashadha":5,"Dhanishta":5,"Uttara Bhadrapada":6,"Rohini":4,"Mrigashira":4,"Punarvasu":6,}, #ardra was 4 but i ignored it
        "antinakshatras":{"Pushya":3,"Swati":3,"Anuradha":4,"Mula":4,"Shravana":3,"Revati":5,},
        "positions":{"Sun in Pisces":6,"Sun in Leo":3,"Moon in Gemini":3,"Moon in Leo":4,"Moon in Aquarius":3,"Venus in Aquarius":5,"Venus in Cancer":3}, #not sure about sun in leo...
        "antipositions":{"Sun in Taurus":6,"Sun in Sagittarius":5,"Sun in Aquarius":4,"Moon in Virgo":8,"Moon in Capricorn":4,"Mercury in Scorpio":7,"Mercury in Capricorn":3,"Venus in Libra":4,"Venus in Scorpio":5,"Venus in Pisces":3,"Venus in Sagittarius":3},
        "aspects":{},
        "antiaspects":{},
        "gates":{},
        "antigates":{},
        "channels": {},
        "antichannels": {},
        "centers":{},
        "anticenters":{},
        "profiles":{},
        "antiprofiles":{},
        "authorities":{},
        "antiauthorities":{},
        "bazisigns":{},
        "antibazisigns":{},
        "color":"#cc99ff",
        "motivation":"",
        "description":"",
        "quotes":{""},
        },
    "sex_symbol":{
     "name":"Sex Symbol",
        "signs":{},
        "antisigns":{},
        "houses":{},
        "antihouses":{},
        "bodies":{},
        "antibodies":{},
        "nakshatras":{},
        "antinakshatras":{},
        "positions":{},
        "antipositions":{},
        "aspects":{},
        "antiaspects":{},
        "gates":{},
        "antigates":{},
        "channels": {},
        "antichannels": {},
        "centers":{},
        "anticenters":{},
        "profiles":{},
        "antiprofiles":{},
        "authorities":{},
        "antiauthorities":{},
        "bazisigns":{},
        "antibazisigns":{},
        "color":"#cc99ff",
        "motivation":"",
        "description":"",
        "quotes":{""},
        },
    "placeholder":{
     "name":"Placeholder",
        "signs":{},
        "antisigns":{},
        "houses":{},
        "antihouses":{},
        "bodies":{},
        "antibodies":{},
        "nakshatras":{},
        "antinakshatras":{},
        "positions":{},
        "antipositions":{},
        "aspects":{},
        "antiaspects":{},
        "gates":{},
        "antigates":{},
        "channels": {},
        "antichannels": {},
        "centers":{},
        "anticenters":{},
        "profiles":{},
        "antiprofiles":{},
        "authorities":{},
        "antiauthorities":{},
        "bazisigns":{},
        "antibazisigns":{},
        "color":"#cc99ff",
        "motivation":"",
        "description":"",
        "quotes":{""},
        },
}
# Local user-uploaded trait storage and scoring helpers.
import ast
import json
import re
from pathlib import Path
from typing import Any, Mapping

from ephemeraldaddy.analysis.weighted_chart_predictor import (
    DEFAULT_CATEGORY_WEIGHTS,
    calculate_weighted_criteria_scores,
    criterion_multiplier_for_target,
)

TRAIT_DIR = Path.home() / ".ephemeraldaddy" / "traits"
TRAIT_FILE_SUFFIX = ".json"

DEFAULT_TRAIT_COLOR = "#cc99ff"
_TRAIT_COLOR_RE = re.compile(r"^#[0-9a-fA-F]{6}$")


def _is_valid_trait_color(color: str) -> bool:
    return bool(_TRAIT_COLOR_RE.match(color.strip()))


def normalize_trait_color(color: str) -> str:
    clean = color.strip()
    if not clean.startswith("#") and re.fullmatch(r"[0-9a-fA-F]{6}", clean):
        clean = f"#{clean}"
    if not _is_valid_trait_color(clean):
        return DEFAULT_TRAIT_COLOR
    return clean.lower()


def _rewrite_single_trait(path: str | Path, profile_updates: Mapping[str, Any]) -> Path:
    source = Path(path)
    profiles = parse_trait_file(source)
    name, profile = next(iter(profiles.items()))
    stored = dict(profile)
    stored.update(profile_updates)
    stored["name"] = name
    source.write_text(
        json.dumps({name: _json_safe_trait_value(stored)}, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    return source


def set_trait_color(path: str | Path, color: str) -> Path:
    return _rewrite_single_trait(path, {"color": normalize_trait_color(color)})


def set_trait_archived(path: str | Path, archived: bool) -> Path:
    return _rewrite_single_trait(path, {"archived": bool(archived)})

_TRAIT_SLUG_RE = re.compile(r"[^a-zA-Z0-9_.-]+")


def traits_dir() -> Path:
    TRAIT_DIR.mkdir(parents=True, exist_ok=True)
    return TRAIT_DIR


def _slugify_trait_name(name: str) -> str:
    slug = _TRAIT_SLUG_RE.sub("_", name.strip()).strip("._-")
    return slug or "trait"


def _unique_trait_path(name: str, *, existing_path: Path | None = None) -> Path:
    base = _slugify_trait_name(name)
    directory = traits_dir()
    candidate = directory / f"{base}{TRAIT_FILE_SUFFIX}"
    index = 2
    while candidate.exists() and (existing_path is None or candidate != existing_path):
        candidate = directory / f"{base}_{index}{TRAIT_FILE_SUFFIX}"
        index += 1
    return candidate


def _strip_line_comments(text: str) -> str:
    """Remove JavaScript-style line comments while preserving string contents."""
    output: list[str] = []
    in_string: str | None = None
    escaped = False
    index = 0
    while index < len(text):
        char = text[index]
        next_char = text[index + 1] if index + 1 < len(text) else ""
        if in_string is None and char == "/" and next_char == "/":
            index += 2
            while index < len(text) and text[index] not in "\r\n":
                index += 1
            continue
        output.append(char)
        if in_string is not None:
            if escaped:
                escaped = False
            elif char == "\\":
                escaped = True
            elif char == in_string:
                in_string = None
        elif char in {"'", '"'}:
            in_string = char
        index += 1
    return "".join(output)


def _extract_literal_from_python(text: str) -> Any:
    cleaned_text = _strip_line_comments(text)
    try:
        return ast.literal_eval(cleaned_text)
    except (SyntaxError, ValueError):
        pass
    module = ast.parse(cleaned_text)
    for node in reversed(module.body):
        value_node: ast.AST | None = None
        if isinstance(node, ast.Assign):
            value_node = node.value
        elif isinstance(node, ast.AnnAssign):
            value_node = node.value
        elif isinstance(node, ast.Expr):
            value_node = node.value
        if value_node is None:
            continue
        try:
            return ast.literal_eval(value_node)
        except (SyntaxError, ValueError):
            continue
    raise ValueError("Could not find a Python literal trait payload.")


def parse_trait_file(path: str | Path) -> dict[str, dict[str, Any]]:
    """Parse a JSON or Similarities Analysis Python export into trait profiles."""
    source = Path(path)
    text = source.read_text(encoding="utf-8")
    if source.suffix.lower() == ".json":
        try:
            payload = json.loads(text)
        except json.JSONDecodeError:
            payload = _extract_literal_from_python(text)
    else:
        payload = _extract_literal_from_python(text)
    if not isinstance(payload, Mapping):
        raise ValueError("Trait file must contain a mapping of trait names to profile data.")
    profiles: dict[str, dict[str, Any]] = {}
    for raw_name, raw_profile in payload.items():
        if not isinstance(raw_profile, Mapping):
            continue
        name = str(raw_profile.get("name") or raw_name).strip()
        if not name:
            continue
        profiles[name] = dict(raw_profile)
        profiles[name]["name"] = name
    if not profiles:
        raise ValueError("Trait file did not contain any usable trait profiles.")
    return profiles



def _json_safe_trait_value(value: Any) -> Any:
    if isinstance(value, Mapping):
        safe: dict[str, Any] = {}
        for raw_key, child in value.items():
            if isinstance(raw_key, tuple) and len(raw_key) == 2:
                key = f"{raw_key[0]}-{raw_key[1]}"
            else:
                key = str(raw_key)
            safe[key] = _json_safe_trait_value(child)
        return safe
    if isinstance(value, tuple):
        return [_json_safe_trait_value(child) for child in value]
    if isinstance(value, list):
        return [_json_safe_trait_value(child) for child in value]
    if isinstance(value, set):
        return [_json_safe_trait_value(child) for child in sorted(value, key=str)]
    return value

def save_trait(name: str, profile: Mapping[str, Any], *, color: str | None = None) -> Path:
    clean_name = name.strip()
    if not clean_name:
        raise ValueError("Trait name cannot be blank.")
    destination = _unique_trait_path(clean_name)
    stored = dict(profile)
    stored["name"] = clean_name
    stored["color"] = normalize_trait_color(str(stored.get("color", DEFAULT_TRAIT_COLOR)))
    stored["archived"] = bool(stored.get("archived", False))
    if color is not None:
        stored["color"] = normalize_trait_color(color)
    elif not _is_valid_trait_color(str(stored.get("color", ""))):
        stored["color"] = DEFAULT_TRAIT_COLOR
    stored["archived"] = bool(stored.get("archived", False))
    destination.write_text(
        json.dumps({clean_name: _json_safe_trait_value(stored)}, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    return destination


def install_trait_file(path: str | Path, name: str, *, color: str | None = None) -> Path:
    profiles = parse_trait_file(path)
    first_profile = next(iter(profiles.values()))
    return save_trait(name, first_profile, color=color)


def list_traits(*, active_only: bool = False) -> list[dict[str, Any]]:
    items: list[dict[str, Any]] = []
    for path in sorted(traits_dir().glob(f"*{TRAIT_FILE_SUFFIX}"), key=lambda p: p.name.casefold()):
        try:
            profiles = parse_trait_file(path)
        except Exception:
            continue
        profile_name, profile = next(iter(profiles.items()))
        archived = bool(profile.get("archived", False))
        if active_only and archived:
            continue
        color = normalize_trait_color(str(profile.get("color", DEFAULT_TRAIT_COLOR)))
        items.append({"name": profile_name, "path": path, "profile": profile, "color": color, "archived": archived})
    return items


def delete_trait(path: str | Path) -> None:
    Path(path).unlink(missing_ok=True)


def rename_trait(path: str | Path, new_name: str) -> Path:
    clean_name = new_name.strip()
    if not clean_name:
        raise ValueError("Trait name cannot be blank.")
    source = Path(path)
    profiles = parse_trait_file(source)
    _old_name, profile = next(iter(profiles.items()))
    destination = _unique_trait_path(clean_name, existing_path=source)
    stored = dict(profile)
    stored["name"] = clean_name
    stored["color"] = normalize_trait_color(str(stored.get("color", DEFAULT_TRAIT_COLOR)))
    stored["archived"] = bool(stored.get("archived", False))
    destination.write_text(
        json.dumps({clean_name: _json_safe_trait_value(stored)}, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    if destination != source:
        source.unlink(missing_ok=True)
    return destination


_TRAIT_SCORE_CATEGORIES = (
    "signs", "antisigns", "bodies", "antibodies", "nakshatras", "antinakshatras",
    "houses", "antihouses", "gates", "antigates", "channels", "antichannels",
    "hdtypes", "antihdtypes", "centers", "anticenters", "profiles", "antiprofiles",
    "authorities", "antiauthorities", "bazisigns", "antibazisigns", "positions",
    "antipositions", "aspects", "antiaspects",
)

_TRAIT_CATEGORY_ALIASES = {
    "antisigns": "signs",
    "antibodies": "bodies",
    "antinakshatras": "nakshatras",
    "antihouses": "houses",
    "antigates": "gates",
    "antichannels": "channels",
    "antihdtypes": "hdtypes",
    "anticenters": "centers",
    "antiprofiles": "profiles",
    "antiauthorities": "authorities",
    "antibazisigns": "bazisigns",
    "antipositions": "positions",
    "antiaspects": "aspects",
}


def _criterion_abs_weight(value: Any) -> float:
    if isinstance(value, Mapping):
        return sum(abs(float(weight)) for weight in value.values() if isinstance(weight, (int, float)))
    if isinstance(value, (set, list, tuple)):
        return float(len(value))
    return 0.0


def _trait_possible_score(profile: Mapping[str, Any]) -> float:
    total = 0.0
    for raw_category in _TRAIT_SCORE_CATEGORIES:
        base_category = _TRAIT_CATEGORY_ALIASES.get(raw_category, raw_category)
        category_weight = float(DEFAULT_CATEGORY_WEIGHTS.get(base_category, 1.0))
        multiplier = float(criterion_multiplier_for_target(profile, base_category))
        total += category_weight * multiplier * _criterion_abs_weight(profile.get(raw_category, {}))
    return max(total, 1.0)


def calculate_trait_likelihoods(chart: Any, traits: list[dict[str, Any]] | None = None) -> dict[str, float]:
    """Return trait scores as 0-100 evidence percentages centered on 50%.

    The weighted predictor returns signed evidence totals.  For Chart View, convert
    those totals into an easier-to-read likelihood-style percentage by comparing
    each signed total with the total absolute criteria weight available for that
    trait profile.  Fifty percent means neutral/no net evidence, higher values mean
    supportive criteria outweighed anti-criteria, and lower values mean anti-criteria
    outweighed supportive criteria.
    """
    trait_items = traits if traits is not None else list_traits(active_only=True)
    trait_items = [item for item in trait_items if not bool(item.get("archived", False))]
    raw_scores = calculate_trait_scores(chart, trait_items)
    likelihoods: dict[str, float] = {}
    for item in trait_items:
        name = str(item.get("name", ""))
        if not name:
            continue
        possible = _trait_possible_score(item.get("profile", {}))
        normalized = max(-1.0, min(1.0, float(raw_scores.get(name, 0.0)) / possible))
        likelihoods[name] = round(50.0 + (normalized * 50.0), 1)
    return likelihoods


def calculate_trait_scores(chart: Any, traits: list[dict[str, Any]] | None = None) -> dict[str, float]:
    trait_items = traits if traits is not None else list_traits(active_only=True)
    trait_items = [item for item in trait_items if not bool(item.get("archived", False))]
    predictors = {item["name"]: item.get("profile", {}) for item in trait_items}
    if not predictors:
        return {}
    raw_scores = calculate_weighted_criteria_scores(chart, predictors=predictors)
    return {str(name): float(raw_scores.get(name, 0.0)) for name in predictors}
