No#troubleshooting: 

If someone imports you inside a locked-down environment (no pip / no network), it’ll blow up on import. That's because of the structure of the core/deps.py dependency autoinstaller.