No#troubleshooting: 

If someone imports you inside a locked-down environment (no pip / no network), install dependencies ahead of time or use the bundled desktop build (`python tools/build_desktop_app.py --onefile`). Runtime auto-install is intentionally disabled.